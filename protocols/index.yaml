# SYNRG Protocol Registry
# Index of extracted protocol modules
# Version: 1.1.0

version: "1.1.0"
last_updated: "2026-01-09"
phase: "2-complete"

# Extracted Protocols (Phase 2 Complete)
# These protocols are now available as standalone modules

protocols:
  # Mandatory Context Delegation Protocol
  - id: mcdp
    name: "Mandatory Context Delegation Protocol"
    status: extracted
    path: "./mcdp.yaml"
    lines: 165
    source_commands:
      - synrg.md
      - synrg-refactor.md
      - synrg-swarm.md
      - synrg-buildworkflow.md
    description: "Enforces sub-agent delegation for context efficiency"

  # Sub-Agent Spawn Protocol
  - id: sub-agent-spawn
    name: "Sub-Agent Spawn Protocol"
    status: extracted
    path: "./sub-agent-spawn.yaml"
    lines: 675
    source_commands:
      - synrg.md
      - synrg-guided.md
      - synrg-refactor.md
    description: "Rules for spawning and managing parallel/sequential sub-agents"

  # Value-First Analysis Protocol
  - id: value-first
    name: "Value-First Analysis Protocol"
    status: extracted
    path: "./value-first.yaml"
    lines: 280
    source_commands:
      - synrg.md
      - synrg-spec.md
    description: "Value-based prioritization and impact assessment"

  # Git Commit Protocol
  - id: git-commit
    name: "Git Commit Protocol"
    status: extracted
    path: "./git-commit.yaml"
    lines: 450
    source_commands:
      - synrg-commit.md
    description: "Git workflow, atomic commits, and message formatting"

  # Phase Gate Protocol
  - id: phase-gate
    name: "Phase Gate Protocol"
    status: extracted
    path: "./phase-gate.yaml"
    lines: 406
    source_commands:
      - synrg-spec.md
      - synrg-scaffold.md
    description: "Phase transition gates and approval requirements"

  # MCP Tool Configuration
  - id: mcp-config
    name: "MCP Tool Configuration Protocol"
    status: extracted
    path: "./mcp-config.yaml"
    lines: 355
    source_commands:
      - synrg.md
      - synrg-buildworkflow.md
      - synrg-swarm.md
    description: "MCP server configuration and delegation patterns"

# Protocol loader documentation
loader: "./loader.md"

# Extraction statistics
statistics:
  total_protocols: 6
  total_lines: 2331
  extraction_date: "2026-01-09"
  extraction_phase: 2

# Protocol import syntax (for Phase 3)
import_syntax: "<!-- PROTOCOL_IMPORT: [protocol_id] -->"

# Schema version for protocol files
protocol_schema_version: "1.0.0"

# Next phase: Path Variables (Phase 3)
next_phase:
  id: 3
  name: "Path Variables"
  description: "Replace hardcoded paths with ${CLAUDE_HOME} variables"
  status: "pending"
