# SYNRG Protocol Registry
# Auto-generated index of extractable protocol modules
# Version: 1.0.0

version: "1.0.0"
last_updated: "2026-01-09"

# Protocols identified for extraction (Phase 2)
# These are duplicated across multiple commands and should be modularized

protocols:
  # Mandatory Context Delegation Protocol
  # Found in: synrg.md, synrg-refactor.md, synrg-swarm.md, synrg-buildworkflow.md
  - id: mcdp
    name: "Mandatory Context Delegation Protocol"
    status: embedded  # embedded | extracted | deprecated
    source_commands:
      - synrg.md
      - synrg-refactor.md
      - synrg-swarm.md
      - synrg-buildworkflow.md
    future_path: "./mcdp.yaml"
    estimated_lines: 45

  # Sub-Agent Spawn Protocol
  # Found in: synrg.md, synrg-guided.md, synrg-refactor.md
  - id: sub-agent-spawn
    name: "Sub-Agent Spawn Protocol"
    status: embedded
    source_commands:
      - synrg.md
      - synrg-guided.md
      - synrg-refactor.md
    future_path: "./sub-agent-spawn.yaml"
    estimated_lines: 80

  # Value-First Analysis Protocol
  # Found in: synrg.md, synrg-spec.md
  - id: value-first
    name: "Value-First Analysis Protocol"
    status: embedded
    source_commands:
      - synrg.md
      - synrg-spec.md
    future_path: "./value-first.yaml"
    estimated_lines: 120

  # Git Commit Protocol
  # Found in: synrg-commit.md
  - id: git-commit
    name: "Git Commit Protocol"
    status: embedded
    source_commands:
      - synrg-commit.md
    future_path: "./git-commit.yaml"
    estimated_lines: 200

  # Phase Gate Protocol
  # Found in: synrg-spec.md, synrg-scaffold.md
  - id: phase-gate
    name: "Phase Gate Protocol"
    status: embedded
    source_commands:
      - synrg-spec.md
      - synrg-scaffold.md
    future_path: "./phase-gate.yaml"
    estimated_lines: 60

  # MCP Tool Configuration
  # Found in: synrg.md, synrg-buildworkflow.md, synrg-swarm.md
  - id: mcp-config
    name: "MCP Tool Configuration"
    status: embedded
    source_commands:
      - synrg.md
      - synrg-buildworkflow.md
      - synrg-swarm.md
    future_path: "./mcp-config.yaml"
    estimated_lines: 150

# Extraction queue (Phase 2 priority order)
extraction_queue:
  - mcdp           # Highest duplication, extract first
  - sub-agent-spawn
  - mcp-config
  - phase-gate
  - value-first
  - git-commit

# Schema version for protocol files
protocol_schema_version: "1.0.0"
